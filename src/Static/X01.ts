import { CheckOut } from "../Components/Enums/X01";

export const maxScoresList = [ 301, 401, 501, 601, 701, 801, 901, 1001 ]
export const checkOutList = Object.values(CheckOut)
export const levelList = Array.from(Array(15).keys())
export const setList = Array.from(Array(15).keys())
export const legsList = Array.from(Array(30).keys())