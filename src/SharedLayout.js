import React, { Component} from "react";
import {hot} from "react-hot-loader";

class SharedLayout extends Component {
    render(){
        return(
            <div class='col-md-12' id="mainLayout">

            </div>
        )
    }
}

export default hot(module)(SharedLayout);